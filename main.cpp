
	fd_set curr_sock,ready_sock;
	struct timeval timeout;


	fd_set curr_sock,ready_sock;
	struct timeval timeout;

	fd_set curr_sock,ready_sock;
	struct timeval timeout;

	fd_set curr_sock,ready_sock;
	struct timeval timeout;
